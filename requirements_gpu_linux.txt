# Core Dependencies
numpy>=1.21.0

# Data Processing
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Visualization
matplotlib>=3.4.0

# Progress Tracking
tqdm>=4.62.0

# Optional: For development
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0

# OpenCV with GPU Support
# Note: The pip version (opencv-contrib-python) does NOT include CUDA support
# For GPU acceleration on Linux, you need to build OpenCV from source with CUDA
# See docs/GPU_ACCELERATION_GUIDE.md for instructions
# For now, installing the standard version for compatibility:
opencv-contrib-python>=4.5.0
